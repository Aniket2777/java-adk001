/**
 * Cross-cutting helpers shared across agents (formatting, date/time conversion, string utilities,
 * etc.). Empty for now - added ahead of need per the agreed folder structure, populated only when a
 * real agent needs a helper that doesn't belong to one specific domain.
 */
package com.shrija.ai.util;
