# Copy this file to .env and fill in real values. .env is for local dev only —
# never commit it. In production, set these as real environment variables instead.

DB_URL=jdbc:mysql://localhost:3306/employee_db
DB_USER=root
DB_PASSWORD=changeme

# Get a free key from https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your-gemini-api-key-here
